# aws-greengrass-lite - AWS IoT Greengrass runtime for constrained devices
# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

# Fleet provisioning requires OpenSSL 3.x (OSSL_PROVIDER) — skip on Android (has 1.1.1)
if(GG_PLATFORM STREQUAL "ANDROID")
  return()
endif()

ggl_init_module(
  fleet-provision
  LIBS gg-sdk
       ggl-common
       aws-iot-call
       core-bus
       ggl-process
       ggl-binpath
       core-bus-gg-config
       core-bus-aws-iot-mqtt
       PkgConfig::openssl
       uuid)
target_compile_definitions(
  fleet-provision
  PRIVATE "GGL_SYSTEMD_SYSTEM_USER=\"${GGL_SYSTEMD_SYSTEM_USER}\"")
target_compile_definitions(
  fleet-provision
  PRIVATE "GGL_SYSTEMD_SYSTEM_GROUP=\"${GGL_SYSTEMD_SYSTEM_GROUP}\"")
